const square = require('./square'); const chai = require('chai'); const assert = chai.assert; const should = chai.should(); const expect = chai.expect; describe('Función cuadrado', function() { it('debería devolver 4 para 2', function() { assert.equal(square(2), 4); }); it('debería devolver 9 para -3', function() { assert.equal(square(-3), 9); }); it('debería devolver 0 para 0', function() { square(0).should.equal(0); }); it('debería devolver 1 para 1', function() { square(1).should.equal(1); }); it('debería devolver 25 para 5', function() { expect(square(5)).to.equal(25); }); it('debería devolver 2.25 para 1.5', function() { expect(square(1.5)).to.equal(2.25); }); });
